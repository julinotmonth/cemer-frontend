[build]
  command = "npm run build"
  publish = "dist"
 
# Wajib untuk React Router (SPA) — semua route diarahkan ke index.html
# supaya refresh di /admin/members, /daftar, dll tidak menghasilkan 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200